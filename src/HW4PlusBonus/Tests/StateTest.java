package HW4PlusBonus.Tests;

import org.junit.Test;

import HW4PlusBonus.State;
import HW4PlusBonus.State;

import static org.junit.Assert.*;

import org.junit.Assert;

/**
 * The Class State.
 */
public class StateTest {

	/*
	 * Not really much that can be done for a class with no output or an abstract class. 
	 * This is just
	 * busy work to test something
	 */

	
	/**
	 * Test Abstract Class "State".
	 */
	@Test
	public void testState() {
		//nothing to test for an abstract class??
		assertTrue(true);
		Assert.assertTrue(!false);
		assertFalse(!(!false)); 
	}

}
